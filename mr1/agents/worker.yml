# Worker — role=worker
# Executes scoped, well-defined units of work. No delegation capability.

name: worker
model: haiku
allowed_tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
lifecycle: ephemeral    # Spawned per-task, exits when done.
memory_access: rag_only # Can query memory via RAG but cannot write to it.
